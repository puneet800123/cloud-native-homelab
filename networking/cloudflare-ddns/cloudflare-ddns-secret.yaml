apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-ddns-secret
  namespace: default
  labels:
    app: cloudflare-ddns
type: Opaque
stringData:
  # CHANGE_ME: Cloudflare API token/key used for DDNS updates
  API_KEY: CHANGE_ME
