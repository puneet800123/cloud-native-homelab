# Placeholder TLS Secret for Logstash (Beats input mTLS).
# Generate real certs (see observability/README.md) and recreate this Secret:
#   kubectl create secret generic logstash-certs \
#     --from-file=ca.crt --from-file=tls.crt --from-file=tls.key
apiVersion: v1
kind: Secret
metadata:
  name: logstash-certs
  namespace: default
type: Opaque
stringData:
  ca.crt: CHANGE_ME
  tls.crt: CHANGE_ME
  tls.key: CHANGE_ME
