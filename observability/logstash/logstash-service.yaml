apiVersion: v1
kind: Service
metadata:
  name: logstash
  namespace: default
spec:
  type: NodePort
  selector:
    app: logstash
  ports:
    - name: beats
      port: 5044
      targetPort: 5044
    - name: monitoring
      port: 9600
      targetPort: 9600
