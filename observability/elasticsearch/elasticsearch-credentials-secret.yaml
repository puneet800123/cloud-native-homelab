apiVersion: v1
kind: Secret
metadata:
  name: elasticsearch-credentials
  namespace: default
  labels:
    app: elasticsearch
type: Opaque
stringData:
  # CHANGE_ME: the 'elastic' superuser password.
  # Referenced by elasticsearch, kibana and logstash.
  password: CHANGE_ME
