apiVersion: v1
kind: Service
metadata:
  name: elasticsearch
  namespace: default
  labels:
    app: elasticsearch
spec:
  type: NodePort
  selector:
    app: elasticsearch
  ports:
    - name: https
      port: 9200
      targetPort: 9200
    - name: transport
      port: 9300
      targetPort: 9300
