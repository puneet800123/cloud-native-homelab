# Placeholder TLS Secret for Elasticsearch.
# Generate real certs (see observability/README.md) and recreate this Secret:
#   kubectl create secret generic elastic-certs \
#     --from-file=ca.crt --from-file=tls.crt --from-file=tls.key
apiVersion: v1
kind: Secret
metadata:
  name: elastic-certs
  namespace: default
  labels:
    app: elasticsearch
type: Opaque
stringData:
  ca.crt: CHANGE_ME
  tls.crt: CHANGE_ME
  tls.key: CHANGE_ME
