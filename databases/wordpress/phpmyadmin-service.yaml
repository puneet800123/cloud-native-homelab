apiVersion: v1
kind: Service
metadata:
  name: phpmyadmin
  namespace: default
spec:
  type: NodePort
  selector:
    app: phpmyadmin
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30081   # access via <NodeIP>:30081
