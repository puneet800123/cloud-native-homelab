apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  labels:
    app: postgres
type: Opaque
stringData:
  # CHANGE_ME: password for the shared Postgres superuser
  # (used by n8n, guacamole, reactive-resume, etc.)
  POSTGRES_PASSWORD: CHANGE_ME
